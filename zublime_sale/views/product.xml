<?xml version="1.0"?>
<odoo>

     <record id="product_template_form_stock" model="ir.ui.view">
        <field name="name">product.template.form.stock</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//label[@for='list_price']" position="before">
                <label for="maximum_price"/>
                <div name="maximum_price">
                    <field name="maximum_price" widget='monetary'
                    attrs="{'invisible': [('product_variant_count', '&gt;', 1), ('is_product_variant', '=', False)]}"
                    options="{'currency_field': 'currency_id', 'field_digits': True}"/>
                </div>
            </xpath>
        </field>
     </record>
</odoo>
